import React from 'react';

const AccordionRadioContent = ({ children }) => {
  return (
    <div className="accordion-radio__content">
      {children}
    </div>
  );
};

export default AccordionRadioContent;
